<UserControl x:Class="BadmintonClub.Views.Tournaments.TournamentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Cột trái: Danh sách giải đấu -->
        <DockPanel Grid.Column="0">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="🔄 Tải lại" 
                        Command="{Binding LoadCommand}" 
                        Margin="0,0,5,0" 
                        Padding="10,5"
                        Background="#2196F3"
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="Bold"
                        Cursor="Hand"/>
                <Button Content="➕ Tạo giải" 
                        Command="{Binding CreateCommand}" 
                        Padding="10,5"
                        Background="#4CAF50"
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="Bold"
                        Cursor="Hand"/>
            </StackPanel>

            <!-- DataGrid với TextBox editable -->
            <DataGrid ItemsSource="{Binding Tournaments}"
                      SelectedItem="{Binding SelectedTournament}"
                      AutoGenerateColumns="False" 
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="All"
                      BorderThickness="1"
                      BorderBrush="#E0E0E0">
                <DataGrid.Columns>
                    <!-- Tên giải - CÓ THỂ EDIT -->
                    <DataGridTemplateColumn Header="Tên giải" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TenGiai}" 
                                           Padding="5"
                                           VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding TenGiai, UpdateSourceTrigger=PropertyChanged}" 
                                         BorderThickness="0"
                                         Background="LightYellow"
                                         Padding="5"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- Ngày -->
                    <DataGridTextColumn Header="Ngày" 
                                        Binding="{Binding NgayBatDau, StringFormat=dd/MM/yyyy}" 
                                        Width="90"
                                        IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Status -->
                    <DataGridTextColumn Header="Status" 
                                        Binding="{Binding Status}" 
                                        Width="70"
                                        IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Nút Lưu -->
                    <DataGridTemplateColumn Header="" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="💾" 
                                        Command="{Binding DataContext.UpdateTournamentNameCommand, 
                                                  RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Width="40"
                                        Height="28"
                                        Background="#4CAF50"
                                        Foreground="White"
                                        BorderThickness="0"
                                        FontSize="16"
                                        Cursor="Hand"
                                        ToolTip="Lưu tên giải"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>

                <DataGrid.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick" 
                                  Command="{Binding OpenDetailCommand}"/>
                </DataGrid.InputBindings>
            </DataGrid>
        </DockPanel>

        <!-- Cột phải: Chi tiết giải đấu -->
        <Border Grid.Column="1" 
                BorderBrush="#E0E0E0" 
                BorderThickness="1" 
                Margin="10,0,0,0" 
                Padding="10"
                Background="White">
            <ContentControl Content="{Binding DetailView}"/>
        </Border>
    </Grid>
</UserControl>
